/**
 * 部署环境配置 —— 部署到公网时，通常只改这一个文件
 *
 * 本文件在 js/config.js **之前**加载，用于覆盖"接口地址自动推导"的结果。
 * 不填任何内容时，一切按 config.js 的自动推导走（本机 / 局域网 / Nginx 同域都适用）。
 *
 * ─────────────────────────────────────────────────────────
 * 什么时候【不用】改？
 *   前端与后端在【同一个域名】下 —— 保持下面那行注释掉即可。
 *     · 本机开发：前端 localhost:9000 + 后端 localhost:8080（自动连 8080）
 *     · 服务器 Nginx：前端 https://你的域名/ + Nginx 把 /api 反代给后端
 *       （此时前端会自动用 https://你的域名/api，见 config.js 的"形态 B"）
 *
 * 什么时候【必须】改？
 *   前端与后端在【不同域名】下 —— 常见于把前端放在免费静态托管、
 *   后端放在 Render / Koyeb 等平台，两边域名不一样。
 *     前端：Netlify / Cloudflare Pages / Render Static Site
 *     后端：https://pms-backend.onrender.com
 *   这时必须显式告诉前端"后端在哪"，否则它会去请求静态托管自己的域名。
 *
 * 改法：把下面这一行前面的 // 去掉，填成你的后端地址（结尾要带 /api）
 * ─────────────────────────────────────────────────────────
 */

// window.PMS_API_BASE = 'https://pms-backend.onrender.com/api';

/**
 * 附加选项（按需使用，一般用不到）
 *
 * 后端只换了端口、域名还是同一个：
 *   window.PMS_API_PORT = 9090;
 *
 * 前端想连本机后端做调试（在浏览器控制台执行也行）：
 *   window.PMS_API_BASE = 'http://localhost:8080/api';
 */
